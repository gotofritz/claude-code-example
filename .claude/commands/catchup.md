# /catchup

Run `git diff main..HEAD --stat` and `git diff main..HEAD`. Compare to `docs/project-plan.md`.

Output: Changes summary, what's complete, in progress, plan discrepancies, next steps. Offer to update plan.
