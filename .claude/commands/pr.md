# /pr

1. Run `/archive` to archive project plan
2. Push branch if needed
3. Create PR: `gh pr create --head {branch} --title "{title}" --body "### Linear Issue 🎟\nRef {branch[:7]}\n### Main Changes 🤹\n{summary}"`
